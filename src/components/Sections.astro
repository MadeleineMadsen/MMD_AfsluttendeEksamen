---
import { Image } from "astro:assets";
import Button from '../components/Button.astro';

export interface Props {
  title: string;
  description: string;
  buttonText: string;
  buttonType: 'primary' | 'secondary';
  image: { src: string; alt: string };
  layout?: 'image-left' | 'image-right'; // Dette gør at man frit kan vælge layout
}

const { title, description, buttonText, buttonType = 'primary', image, layout = 'image-left' } = Astro.props;
---
<div class={`mx-auto flex flex-col ${layout === 'image-left' ? 'md:flex-row' : 'md:flex-row-reverse'} items-center justify-center px-s py-xl`}>
  <!-- Billede -->
  <div class="md:w-1/2 flex justify-center items-center mb-m md:mb-0 md:p-s">
    <Image src={image.src} alt={image.alt} class="object-obtain w-full md:w-auto h-48 md:h-full" />
  </div>
  
  <!-- Tekstindhold -->
  <div class="font-poppins text-pdefault w-full md:w-1/2 text-center md:text-left md:ml-xxl ml-0 md:mb-m mb-0">
    <h3 class="text-mnormalh3 md:text-normalh3 font-poppins font-semibold md:mb-m mb-0">{title}</h3>
    <p class="text-mpnormal md:pnormal md:mb-m mb-0">{description}</p>
    <a href=""><Button variant={buttonType}>{buttonText}</Button></a>
  </div>
</div>
